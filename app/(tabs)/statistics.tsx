import { Text, View } from "react-native";

export default function Statistics() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text className=''>Stats Screen</Text>
    </View>
  );
}
