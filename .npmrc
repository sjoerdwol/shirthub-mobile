# Expo SDK 56: expo-router pulls react-server-dom-webpack (optional peer) which
# requests react@^19.2.7 while Expo pins react@19.2.3. This is an upstream
# version-range quirk; legacy-peer-deps lets `npm install` resolve it cleanly.
legacy-peer-deps=true
